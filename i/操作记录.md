# 操作记录

## 2026-05-29

### 任务：创建 OpenGL ES 3.0 兼容着色器

#### 已读取的原始文件
- 18 个桌面 OpenGL 着色器文件（9 对 .vert/.frag）
- GLShaderUtil.h / GLShaderUtil.cpp - 着色器编译工具
- GLSLUtil.h / GLSLUtil.cpp - 着色器加载/预处理框架
- GLObject.h - GL 对象封装

#### 已创建的 GLES 3.0 着色器文件 (18 个)
所有文件位于 `/workspace/viewer/Saba/Viewer/resource/shader/`，以 `_gles` 后缀命名。

转换内容：
1. `#version 140` → `#version 300 es`
2. 所有 Fragment Shader 添加 `precision highp float;`
3. 顶点属性使用 `layout(location = N) in` 显式指定位置
4. mmd.frag：`sampler2DShadow` + `textureProj()` → `sampler2D` + 手动深度比较

#### 已创建的工具类
- `/workspace/viewer/Saba/GL/GLES_ShaderUtil.h`
- `/workspace/viewer/Saba/GL/GLES_ShaderUtil.cpp`

`GLES_ShaderUtil` 与 `GLSLShaderUtil` 接口一致，自动加载 `_gles` 后缀的着色器文件。

#### 已创建的文档
- `/workspace/i/项目说明.md`
- `/workspace/i/项目技术细节.md`
- `/workspace/i/操作记录.md`